---
// src/components/Tabs/TabList.astro
export interface Props {
	label: string;
	class?: string;
}

const { label, class: className = "" } = Astro.props;
---

<div role="tablist" aria-label={label} class:list={["tabs-list", className]}>
	<slot />
</div>

<style>
	.tabs-list {
		width: fit-content;
		display: flex;
		align-self: center;
		flex-wrap: wrap;
		gap: var(--tabs-gap, 0.25rem);
		padding: var(--tabs-padding-to-wrapper, 0.25rem);
		margin: 0;
		list-style: none;
		background: var(--tabs-bg);
		-webkit-backdrop-filter: blur(4px);
		backdrop-filter: blur(4px);
		box-shadow: var(--shadow-main);
		border-radius: calc(var(--tabs-radius) + var(--tabs-padding-to-wrapper));
		@supports (corner-shape: squircle) {
			corner-shape: squircle;
		}
	}

	/* Default variant */
	:global(.tabs--default) .tabs-list {
		background: var(--tabs-bg);
		/* border: 1px solid var(--tabs-border-color); */
	}

	/* Pills variant */
	:global(.tabs--pills) .tabs-list {
		background: transparent;
		border: none;
		padding: 0;
	}

	/* Underline variant */
	:global(.tabs--underline) .tabs-list {
		background: transparent;
		border: none;
		border-bottom: 2px solid var(--tabs-border-color);
		border-radius: 0;
		padding: 0;
		gap: 0;
	}

	/* Vertical variant */
	:global(.tabs--vertical) .tabs-list {
		flex-direction: column;
		flex-shrink: 0;
		width: auto;
		min-width: 100px;
		border-radius: calc(var(--tabs-radius) + var(--tabs-padding-to-wrapper));
		@supports (corner-shape: squircle) {
			corner-shape: squircle;
		}
	}

	/* Full width tabs */
	:global(.tabs--full-width) .tabs-list {
		width: 100%;
	}

	:global(.tabs--full-width) .tabs-list > :global(*) {
		flex: 1;
	}
</style>
